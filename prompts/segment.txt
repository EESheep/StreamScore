你是一个直播内容分析师。下面是按时间排列的主播发言片段，每个片段包含起止时间和转写文本。
请根据主题变化，将内容切分为不同的段落。段落应围绕一个明确的话题。
输出 JSON 数组，每个元素包含：
- "start": 开始时间（秒，取该段落第一条记录的 start）
- "end": 结束时间（秒，取该段落最后一条记录的 end）
- "title": 简短的段落标题（10字以内）
- "summary": 1-2句话摘要（50字以内）

只输出 JSON 数组，不要包含任何其他文字。

内容如下：
{{ transcript_json }}
